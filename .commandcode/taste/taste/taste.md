# Taste
- Uses `uv` for Python environment and package management (installed Python via uv; prefers uv's workflow — `uv init`, `uv add`, `uv run` — over pip/venv). Confidence: 0.8
- Prefers to run shell commands himself and paste the output back to the assistant (asked "give me the command... i will run and give you the output first"; then activated the venv, ran `uv pip list` himself, and pasted the full output) rather than having the agent execute commands directly. Exception: he's comfortable letting the agent execute routine git operations (stage/commit/push) when he explicitly approves them — said "yes" to the agent committing `.gitignore` and `.env.example` and pushing to origin. Confidence: 0.7
- Prefers to write his own code/scripts himself instead of having the assistant generate them (declined the offer to write the agent file: "no, i will write by my self"). Offer code only when he asks for it. Confidence: 0.8
- Wants his practice scripts to stay consistent with each other — when a setup (e.g., OpenRouter LLM config) is established in one file, he asks to apply the same pattern "also" to his other scripts, expecting the identical env-var names and structure across all of them. Treats one working project's `.env` (e.g., CrewAI-Agents) as the canonical reference for valid credentials/config and points to it to fix other projects' configs, expecting the assistant to map differing env-var names across projects (e.g., `LLM_*` vs `OPENROUTER_*`) rather than asking which values to use. Confidence: 0.7
- Wants explanations of tooling-generated project files (`.venv`, `pyproject.toml`, `uv.lock`, `.python-version`, `src/`) — what each one does and which are required vs optional — before proceeding. Confidence: 0.6
- Wants project setup/tooling explanations persisted in the README (or other docs) so he can re-understand things later ("update all these details in readme file as well, so that i will understand in future"; again asked to "update accordingly readme" when the setup changed). Confidence: 0.8
- Prefers OpenRouter as the LLM API provider instead of OpenAI — one key gives access to many models (uses `OPENROUTER_API_KEY` env var and `openrouter/` model prefix via LiteLLM). Uses it with his own personal key for self-learning of AI frameworks (CrewAI, DeepEval, LangGraph practice) because the company doesn't provide paid model access — learning only, not for company projects. Confidence: 0.95
- Works on a corporate network with Zscaler TLS inspection: HTTPS calls from Python (httpx/openai/urllib) fail with `CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate` because Zscaler presents its own CA (Zscaler Intermediate Root CA) for external sites like openrouter.ai. Not fixable from code — needs the Zscaler CA added to the trust store or the domain exempted by IT; expect this to affect any external API call on this machine. Confidence: 0.9
- Wants secret/credential hygiene in projects: asked to "add git ignore and add .env file init" — expects a `.gitignore` covering `.env`, `.venv/`, `__pycache__/`, IDE/OS files, plus a `.env.example` template documenting the required env vars (e.g., `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`, `BASE_URL`) so the real `.env` with actual keys stays untracked. Confidence: 0.8
- When a blocker requires action from another party (e.g., IT/security for the Zscaler SSL exemption), wants a ready-to-send, professionally-worded email draft to escalate the request, framed around his context (personal learning use, no company data). Confidence: 0.6
- Prefers remediating an accidentally-committed secret by scrubbing it from git history (rewrite commit, `git reflog expire --expire=now --all`, `git gc --prune=now`, verify with `git log -S <key>`) over rotating the key — "remove the previously committed one, i dont want to provide new key". When the secret never reached the remote, he's content to keep using the existing key rather than generate a new one. Confidence: 0.7
- Integrates external SaaS tools (e.g., Atlassian Jira) into his dev environment via MCP servers; provides the raw connection details up front (site base URL, login email) in a terse message and expects the assistant to research and pick the right server/auth approach (e.g., official Atlassian MCP with browser OAuth) and configure it, rather than asking him which method to use. When the assistant offers alternative setups, he defers the decision back to the assistant ("which one do you think i need to keep?") and expects a clear, decisive recommendation. Confidence: 0.7
- Prefers configuring redundant/backup integration paths over minimal config — when offered the option to also enable the MCP Bearer-token auth alongside the REST basic-auth fallback, said "yes... i think it will usefull"; tends to accept extra setup/credential options that future-proof a config. Confidence: 0.5
- Cares about authentication friction in tooling — questioned whether the OAuth MCP setup would prompt for login on every use ("everytime when i hit in github chat, it will ask for login ? which is not good right?"), showing he expects integrations to work with a one-time login and persistent session rather than recurring manual prompts. Confidence: 0.4
